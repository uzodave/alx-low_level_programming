#include "main.h"

/**
 * _abs - check the code and print the absolute value of an integer
 * @n: variable for integers
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
	return (n);
	}
	return (0);
	}
